diff --git a/node_modules/zudoku/src/lib/authentication/providers/openid.tsx b/node_modules/zudoku/src/lib/authentication/providers/openid.tsx
index afe6ae0..0bc565a 100644
--- a/node_modules/zudoku/src/lib/authentication/providers/openid.tsx
+++ b/node_modules/zudoku/src/lib/authentication/providers/openid.tsx
@@ -17,7 +17,7 @@ import { LogoutCallbackHandler } from "../components/LogoutCallbackHandler.js";
 import { OAuthErrorPage } from "../components/OAuthErrorPage.js";
 import { fetchServerSession } from "../cookie-sync.js";
 import { DEFAULT_SESSION_MAX_AGE, SESSION_ENDPOINT_PATH } from "../cookies.js";
-import { AuthorizationError, OAuthAuthorizationError } from "../errors.js";
+import { AuthorizationError } from "../errors.js";
 import { type UserProfile, useAuthState } from "../state.js";
 import { redirectToSignUpUrl } from "./util.js";
 
@@ -178,19 +178,6 @@ export class OpenIDAuthenticationProvider
       throw new AuthorizationError("No expires_in in response");
     }
 
-    const accessToken = response.access_token;
-    if (accessToken.split(".").length !== 3) {
-      throw new OAuthAuthorizationError(
-        "The access token received is not a valid JWT.",
-        {
-          error: "configuration_error",
-          error_description:
-            "The authentication provider is issuing opaque tokens instead of JWTs. " +
-            "Ensure you have configured the correct `audience` in your authentication settings.",
-        },
-      );
-    }
-
     const claims = response.id_token
       ? oauth.getValidatedIdTokenClaims(response)
       : undefined;
diff --git a/node_modules/zudoku/src/lib/components/navigation/useNavigationFrame.ts b/node_modules/zudoku/src/lib/components/navigation/useNavigationFrame.ts
index a3706347..d62d330b 100644
--- a/node_modules/zudoku/src/lib/components/navigation/useNavigationFrame.ts
+++ b/node_modules/zudoku/src/lib/components/navigation/useNavigationFrame.ts
@@ -59,2 +59,2 @@ const resolveBack = (item: NavigationItem): BackLink => {
-// Cross-section stack: derive the back link from the ancestor that owns the `stack` link.
+// Cross-section stack: derive the back link from the nearest ancestor that owns the `stack` link.
 export const resolveStackOwner = (
@@ -67,3 +67,3 @@ export const resolveStackOwner = (
     if (pathname !== base && !pathname.startsWith(`${base}/`)) return;
-    return resolveBack(parents.at(0) ?? item);
+    return resolveBack(parents.at(-1) ?? item);
   });
