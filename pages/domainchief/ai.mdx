---
title: AI agents
description: Connect an MCP-compatible AI client to Domain Chief.
---

import {Head} from "zudoku/components";

<Head>
  <title>AI agents | Domain Chief</title>
</Head>

Domain Chief has a remote MCP server for AI clients. Once connected, an agent can inspect domains, contacts, TLDs, DNS records, and web redirects. Write access depends on the scopes you approve.

## Server URL

```text
https://domain.chief.app/mcp
```

## Connect a client

MCP clients use different names for their integration settings, but the connection follows the same steps:

1. Open the client's MCP or integrations settings.
2. Add a remote MCP server with the URL above.
3. Sign in with your Chief account.
4. Review and approve the requested scopes.

The authorization screen currently treats requested scopes as one set. You approve the full set or cancel the connection.

## Available tools

The server registers only the tools allowed by the approved scopes.

### TLDs and contacts

| Tool | Action | Required scope |
|---|---|---|
| `list-tlds` | List supported TLDs and indicative pricing | `domainchief:tlds:read` |
| `get-tld` | Retrieve one TLD | `domainchief:tlds:read` |
| `list-contacts` | List team contacts | `domainchief:contacts:read` |
| `get-contact` | Retrieve one contact | `domainchief:contacts:read` |

### Domains

| Tool | Action | Required scope |
|---|---|---|
| `list-domains` | List team domains | `domainchief:domains:read` |
| `update-domain` | Update an existing domain | `domainchief:domains:write` |
| `check-domain-availability` | Check whether a domain can be registered or transferred | `domainchief:domains:read:availability` |

### DNS records

These tools work only with domains using Domain Chief hosted DNS.

| Tool | Action | Required scope |
|---|---|---|
| `get-dns-record` | Retrieve one DNS record | `domainchief:dns:read` |
| `list-dns-records` | List DNS records | `domainchief:dns:read` |
| `create-dns-record` | Create a DNS record | `domainchief:dns:write` |
| `update-dns-record` | Update a DNS record | `domainchief:dns:write` |
| `delete-dns-record` | Delete a DNS record | `domainchief:dns:write` |
| `clear-dns-records` | Remove DNS records except records managed by redirects | `domainchief:dns:write` |
| `reset-dns-records` | Restore the Domain Chief default records | `domainchief:dns:write` |

### Web redirects

| Tool | Action | Required scope |
|---|---|---|
| `get-web-redirect` | Retrieve one web redirect | `domainchief:redirects_web:read` |
| `list-web-redirects` | List web redirects | `domainchief:redirects_web:read` |
| `create-web-redirect` | Create a web redirect | `domainchief:redirects_web:write` |
| `update-web-redirect` | Update a web redirect | `domainchief:redirects_web:write` |
| `delete-web-redirect` | Delete a web redirect | `domainchief:redirects_web:write` |

## Try a request

After connecting, ask the client to perform a specific task:

- Check whether `copper-lantern.com` is available.
- List the DNS records for one of my domains.
- Change the `tag` metadata on this domain to `staging`.
- Create a redirect from `www` to the root domain.

Review proposed write operations before allowing the agent to continue.

## Change permissions

Start with read scopes when possible. If a required tool is missing, reconnect and authorize the client with the additional scope. Domain Chief does not add scopes to an existing connection without another authorization step.

See [Domain Chief API scopes](/domainchief/api/scopes) for the full scope hierarchy.

## MCP or REST API

Use MCP when an AI client needs to choose and call Domain Chief tools. Use the [REST API](/domainchief/api/introduction) for application code, fixed workflows, or endpoints that the MCP server does not expose.
